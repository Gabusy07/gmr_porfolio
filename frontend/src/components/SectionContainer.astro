---

interface Props {
  id: string;
  title:string;
  className?: string;
}

const {id, title, className }=  Astro.props;

---

<section id={id} class={`p-3 border border-green-500 ${className}`}>
    <div class="container">
      <h2>
        {title}
      </h2>
    </div>
   <slot/> 
</section>

<style>
  section{
    max-width: 700px;
    margin: 0 auto 72px;
  }
</style>